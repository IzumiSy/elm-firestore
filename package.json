{
  "private": true,
  "scripts": {
    "build": "cd lib && elm make",
    "publish": "cd lib && elm publish",
    "test": "cd lib && elm-test",
    "test:integration:setup": "firebase setup:emulators:firestore",
    "test:integration": "./integrations/test.sh"
  },
  "devDependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.3",
    "elm-test": "^0.19.1-revision2",
    "firebase-tools": "^9.9.0",
    "mocha": "^8.3.2"
  }
}
