{
  "private": true,
  "scripts": {
    "build": "elm make src/Firestore.elm",
    "test": "elm-test",
    "publish": "elm publish"
  },
  "devDependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.3",
    "elm-test": "^0.19.1"
  }
}
